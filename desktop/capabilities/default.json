{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Minimal permissions for the local studio window",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-close"]
}
