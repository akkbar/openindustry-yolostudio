# Development data

This directory is reserved for local fixtures and ignored scratch data. Application data is stored in `%LOCALAPPDATA%\VisionStudio`, regardless of the current working directory. No database is introduced before Phase 5.
